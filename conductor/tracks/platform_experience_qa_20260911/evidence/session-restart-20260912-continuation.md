---
type: evidence-receipt
track: platform_experience_qa_20260911
recorded_on: 2026-09-12
observed_at: 2026-09-12T11:11:31Z
status: restart_continuation_read_only
---

# Session restart continuation

This continuation records the next bounded restart pass after the original
live-restart receipt. It reconciles the actual Codex task handles and the
current worktree/process state. No source download, database, object-store,
writer, scheduler, deployment, remote publication or push action occurred.

## Botanical implementation owner

The archived task `01a092bd-c71d-7bb3-bc46-e0dac684f751` (Build botanical
species profiles) was reopened and sent a current-main, read-only reconciliation
prompt. Two bounded polls observed the turn `inProgress` without an assistant
message, tool marker or revision change. A stop checkpoint was then delivered to
the verified task handle. The task completed the checkpoint and returned `idle`.

Its report rechecked current `main` at `b311e208`, the preserved implementation
`edc6afd`, the superseded documentation branch `3135d6b`, the original missing
Git metadata and the absence of a matching botanical process. It made no file or
ref changes. The report retains the exact source and tree identities and keeps
the following gates open:

- a separately authorized bounded read-only census of `agri.species` and
  `agri.companion_relationships`;
- independent admission and custody of WCVP, USDA, TRY, FEIS and other
  non-Herbaria profile fields;
- WTU/UBC Herbaria release, rights, coordinate policy and quarantine controls;
- occurrence-plane release, publication and specimen uncertainty handling; and
- immutable profile publication plus production API, agent and MCP parity.

The existing exact-UUID transitional lookup and current census receipt remain
canonical. `edc6afd` is still an unaccepted broad candidate and `3135d6b` is
still superseded; neither was integrated. The completed restart task may be
archived again after this lifecycle record is retained, while the source,
census and production owners remain open.

## Ingestion owner

The unresolved task `01a08b00-2a50-73c2-b39b-39523c74ceb2` was restarted with
a concrete read-only preflight instruction. Its turn completed after the
bounded wait without an assistant message, tool marker, command result or
revision. It is idle and unresolved. Its historical candidate remains
unintegrated and its task is retained rather than archived. No Python,
`python3`, or matching data-writer process was observed in the local process
check.

## New visual owner

A dedicated isolated current-main task was requested for the remaining
presentation-only visual-layer work:

`client-new-thread:9f7291c1-48bc-4e45-bfe1-4845d2d80df8`

Its scope is to inspect the active multiscale and QA tracks and implement only a
small, evidence-backed visual slice beyond the integrated scalar-label and
weather style-readiness repairs. The task is forbidden from touching data,
Parquet, databases, ingestion, writers, Railway, deployment or push. The alias
was not present in `list_threads` at this observation, so it is a queue alias,
not a task identity or completion evidence. Do not create a duplicate until its
materialization state is known.

## Lifecycle decision

The botanical restart is complete and retained evidence is sufficient for
archival of that task. The ingestion owner is unresolved and remains open. The
visual task is pending materialization. The platform QA track remains active;
forecast, source admission, populated-data, mobile, agent/MCP and production
gates remain unchanged.
