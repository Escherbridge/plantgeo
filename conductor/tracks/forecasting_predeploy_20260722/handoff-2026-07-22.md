---
type: handoff
---

# Continuation handoff

Start from `plan.md` and `continuation-evidence-2026-07-22.md` in this directory.
Preserve the intentionally dirty checkout and all warehouse history. Do not reset,
broadly stage, delete volumes/caches, deploy to Railway, schedule jobs, or reinterpret
the rejected v1 evidence.

Revision `20260722_0008`, versioned digests, active-policy/calibration enforcement,
staged-v2 insert enforcement, immutable finalized-receipt policies, function privilege
hardening, and split production service profiles are implemented and validated in
disposable PostgreSQL 16 databases. Every named final rehearsal database was dropped
after exact-name/owner checks. The live `plantgeo` warehouse was not migrated by this
continuation and remains the canonical rejected-v1 source.

The next bounded work is external-state dependent: provide a safe PostgreSQL 18 target
for the extension/restore drill and a verified post-receipt clone containing the 14
canonical v1 receipts. The corrected v2 real-history fixture still has not executed;
run it only under new governed identities or in a disposable database and publish only
if every stricter gate passes. Use ignored local credentials without printing them.
Treat immutable hindcast receipts as canonical and `job_output` only as explanatory
audit text.
