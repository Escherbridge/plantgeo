"""Agri Data Service Interface Layer (L4).

Command-line entry points (`interface/cli`) and HTTP API routes (`interface/http`).
May import all lower layers (`foundation`, `method`, `warehouse`, `pipeline`, `planes`).
"""
