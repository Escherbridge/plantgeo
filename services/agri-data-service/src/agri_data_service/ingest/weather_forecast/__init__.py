"""The `weather-forecast` layer's source domain: Open-Meteo NWP run fetch and its typed contract.

A domain package under `ingest/` (`tests/test_layer_import_contract.py::test_domain_packages_do_not_import_each_other`),
so nothing here or under a future sibling domain may import the other by name. See `AGENTS.md`.
"""

from __future__ import annotations
