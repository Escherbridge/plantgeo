-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE TABLE agri.release_set_item (
    release_set_id uuid NOT NULL,
    source_release_id uuid NOT NULL,
    source_role character varying(100) DEFAULT 'input'::character varying NOT NULL,
    added_at timestamp with time zone DEFAULT now() NOT NULL
);

-- CONSTRAINT: release_set_item pk_release_set_item
ALTER TABLE ONLY agri.release_set_item
    ADD CONSTRAINT pk_release_set_item PRIMARY KEY (release_set_id, source_release_id);
