-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE TABLE agri.publication_pointer (
    id uuid DEFAULT gen_random_uuid() NOT NULL,
    product character varying(150) NOT NULL,
    scope_key character varying(500) NOT NULL,
    job_output_id uuid NOT NULL,
    previous_job_output_id uuid,
    release_set_id uuid,
    revision bigint NOT NULL,
    published_at timestamp with time zone DEFAULT now() NOT NULL,
    published_by character varying(255) NOT NULL,
    created_at timestamp with time zone DEFAULT now() NOT NULL,
    updated_at timestamp with time zone DEFAULT now() NOT NULL,
    CONSTRAINT ck_publication_pointer_positive_publication_revision CHECK ((revision > 0)),
    CONSTRAINT ck_publication_pointer_publication_previous_differs CHECK (((previous_job_output_id IS NULL) OR (previous_job_output_id <> job_output_id)))
);

-- CONSTRAINT: publication_pointer pk_publication_pointer
ALTER TABLE ONLY agri.publication_pointer
    ADD CONSTRAINT pk_publication_pointer PRIMARY KEY (id);

-- CONSTRAINT: publication_pointer uq_publication_pointer_product_scope
ALTER TABLE ONLY agri.publication_pointer
    ADD CONSTRAINT uq_publication_pointer_product_scope UNIQUE (product, scope_key);
