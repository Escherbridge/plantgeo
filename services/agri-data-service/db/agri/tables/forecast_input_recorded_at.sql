-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE TABLE agri.forecast_input_recorded_at (
    input_kind character varying(64) NOT NULL,
    input_key text NOT NULL,
    recorded_at timestamp with time zone NOT NULL
);

-- CONSTRAINT: forecast_input_recorded_at forecast_input_recorded_at_pkey
ALTER TABLE ONLY agri.forecast_input_recorded_at
    ADD CONSTRAINT forecast_input_recorded_at_pkey PRIMARY KEY (input_kind, input_key);
