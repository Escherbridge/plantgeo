-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE TABLE agri.forecast_candidate_evaluation_origin (
    id bigint NOT NULL,
    evaluation_id uuid NOT NULL,
    origin_cutoff_time timestamp with time zone NOT NULL,
    fold_kind character varying(20) NOT NULL,
    scored_step_count integer NOT NULL,
    mean_absolute_error double precision NOT NULL,
    root_mean_squared_error double precision NOT NULL,
    bias double precision NOT NULL,
    interval_coverage_fraction double precision,
    skill_versus_persistence double precision,
    origin_checksum character varying(64) NOT NULL,
    created_at timestamp with time zone DEFAULT now() NOT NULL,
    CONSTRAINT ck_forecast_candidate_evaluation_origin_checksum CHECK (((origin_checksum)::text ~ '^[0-9a-f]{64}$'::text)),
    CONSTRAINT ck_forecast_candidate_evaluation_origin_coverage_fraction CHECK (((interval_coverage_fraction IS NULL) OR ((interval_coverage_fraction >= (0.0)::double precision) AND (interval_coverage_fraction <= (1.0)::double precision)))),
    CONSTRAINT ck_forecast_candidate_evaluation_origin_fold_kind CHECK (((fold_kind)::text = ANY (ARRAY[('development'::character varying)::text, ('final_holdout'::character varying)::text]))),
    CONSTRAINT ck_forecast_candidate_evaluation_origin_nonnegative_errors CHECK (((mean_absolute_error >= (0.0)::double precision) AND (root_mean_squared_error >= (0.0)::double precision))),
    CONSTRAINT ck_forecast_candidate_evaluation_origin_positive_steps CHECK ((scored_step_count > 0))
);

-- IDENTITY: forecast_candidate_evaluation_origin_id_seq
ALTER TABLE agri.forecast_candidate_evaluation_origin ALTER COLUMN id ADD GENERATED ALWAYS AS IDENTITY (
    SEQUENCE NAME agri.forecast_candidate_evaluation_origin_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

-- CONSTRAINT: forecast_candidate_evaluation_origin pk_forecast_candidate_evaluation_origin
ALTER TABLE ONLY agri.forecast_candidate_evaluation_origin
    ADD CONSTRAINT pk_forecast_candidate_evaluation_origin PRIMARY KEY (id);

-- CONSTRAINT: forecast_candidate_evaluation_origin uq_forecast_candidate_evaluation_origin_identity
ALTER TABLE ONLY agri.forecast_candidate_evaluation_origin
    ADD CONSTRAINT uq_forecast_candidate_evaluation_origin_identity UNIQUE (evaluation_id, origin_cutoff_time);
