-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE FUNCTION agri.forecast_iteration_value_checksum(p_valid_time timestamp with time zone, p_horizon_step integer, p_low_value double precision, p_median_value double precision, p_high_value double precision, p_increment_count integer, p_parameter_checksum character varying) RETURNS character varying
    LANGUAGE sql IMMUTABLE
    SET "TimeZone" TO 'UTC'
    SET "DateStyle" TO 'ISO, MDY'
    SET "IntervalStyle" TO 'postgres'
    SET extra_float_digits TO '1'
    AS $$
            SELECT encode(
                digest(
                    concat_ws(
                        '|',
                        'forecast_iteration_value_v1',
                        p_valid_time::text,
                        p_horizon_step::text,
                        p_low_value::text,
                        p_median_value::text,
                        p_high_value::text,
                        p_increment_count::text,
                        p_parameter_checksum
                    ),
                    'sha256'
                ),
                'hex'
            )::varchar
        $$;
