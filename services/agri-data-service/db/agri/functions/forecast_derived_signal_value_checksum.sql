-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE FUNCTION agri.forecast_derived_signal_value_checksum(p_signal_definition_id uuid, p_series_key character varying, p_origin_cutoff_time timestamp with time zone, p_valid_time timestamp with time zone, p_availability_time timestamp with time zone, p_lineage_depth integer, p_signal_value double precision, p_input_release_checksum character varying, p_recipe_checksum character varying) RETURNS character varying
    LANGUAGE sql IMMUTABLE
    SET "TimeZone" TO 'UTC'
    SET "DateStyle" TO 'ISO, MDY'
    SET "IntervalStyle" TO 'postgres'
    SET extra_float_digits TO '1'
    SET search_path TO 'public', 'pg_catalog'
    AS $$
            SELECT encode(
                digest(
                    concat_ws(
                        '|',
                        'forecast_derived_signal_value_v1',
                        p_signal_definition_id::text,
                        p_series_key,
                        p_origin_cutoff_time::text,
                        p_valid_time::text,
                        p_availability_time::text,
                        p_lineage_depth::text,
                        coalesce(p_signal_value::text, 'null'),
                        p_input_release_checksum,
                        p_recipe_checksum
                    ),
                    'sha256'
                ),
                'hex'
            )::varchar
        $$;
