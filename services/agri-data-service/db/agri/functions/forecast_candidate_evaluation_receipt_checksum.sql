-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

CREATE FUNCTION agri.forecast_candidate_evaluation_receipt_checksum(p_evaluation_key character varying, p_series_key character varying, p_candidate_family character varying, p_candidate_version character varying, p_hyperparameters jsonb, p_simulation_seed bigint, p_export_manifest_checksum character varying, p_horizon_steps integer, p_development_origin_count integer, p_final_holdout_origin_count integer, p_decision character varying) RETURNS character varying
    LANGUAGE sql IMMUTABLE
    SET "TimeZone" TO 'UTC'
    SET "DateStyle" TO 'ISO, MDY'
    SET "IntervalStyle" TO 'postgres'
    SET extra_float_digits TO '1'
    SET search_path TO 'public', 'pg_catalog'
    AS $$
            SELECT encode(
                digest(
                    concat_ws(
                        '|',
                        'forecast_candidate_evaluation_v1',
                        p_evaluation_key,
                        p_series_key,
                        p_candidate_family,
                        p_candidate_version,
                        p_hyperparameters::text,
                        p_simulation_seed::text,
                        p_export_manifest_checksum,
                        p_horizon_steps::text,
                        p_development_origin_count::text,
                        p_final_holdout_origin_count::text,
                        p_decision
                    ),
                    'sha256'
                ),
                'hex'
            )::varchar
        $$;
