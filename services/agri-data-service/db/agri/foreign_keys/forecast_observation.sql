-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_observation forecast_observation_entity_state_id_fkey
ALTER TABLE ONLY agri.forecast_observation
    ADD CONSTRAINT forecast_observation_entity_state_id_fkey FOREIGN KEY (entity_state_id) REFERENCES agri.forecast_entity_state(id);

-- FK CONSTRAINT: forecast_observation forecast_observation_series_id_fkey
ALTER TABLE ONLY agri.forecast_observation
    ADD CONSTRAINT forecast_observation_series_id_fkey FOREIGN KEY (series_id) REFERENCES agri.forecast_series(id);

-- FK CONSTRAINT: forecast_observation forecast_observation_source_release_id_fkey
ALTER TABLE ONLY agri.forecast_observation
    ADD CONSTRAINT forecast_observation_source_release_id_fkey FOREIGN KEY (source_release_id) REFERENCES agri.source_release(id);
