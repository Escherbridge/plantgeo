-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_candidate_evaluation fk_forecast_candidate_evaluation_definition
ALTER TABLE ONLY agri.forecast_candidate_evaluation
    ADD CONSTRAINT fk_forecast_candidate_evaluation_definition FOREIGN KEY (signal_definition_id) REFERENCES agri.forecast_signal_definition(id);

-- FK CONSTRAINT: forecast_candidate_evaluation fk_forecast_candidate_evaluation_series
ALTER TABLE ONLY agri.forecast_candidate_evaluation
    ADD CONSTRAINT fk_forecast_candidate_evaluation_series FOREIGN KEY (series_id) REFERENCES agri.forecast_series(id);
