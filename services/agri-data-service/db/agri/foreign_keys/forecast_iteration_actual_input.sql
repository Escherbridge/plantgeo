-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_iteration_actual_input forecast_iteration_actual_input_actual_id_fkey
ALTER TABLE ONLY agri.forecast_iteration_actual_input
    ADD CONSTRAINT forecast_iteration_actual_input_actual_id_fkey FOREIGN KEY (actual_id) REFERENCES agri.forecast_iteration_actual(id);

-- FK CONSTRAINT: forecast_iteration_actual_input forecast_iteration_actual_input_source_release_id_fkey
ALTER TABLE ONLY agri.forecast_iteration_actual_input
    ADD CONSTRAINT forecast_iteration_actual_input_source_release_id_fkey FOREIGN KEY (source_release_id) REFERENCES agri.source_release(id);
