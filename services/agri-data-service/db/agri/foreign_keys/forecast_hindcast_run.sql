-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_hindcast_run forecast_hindcast_run_forecast_run_id_fkey
ALTER TABLE ONLY agri.forecast_hindcast_run
    ADD CONSTRAINT forecast_hindcast_run_forecast_run_id_fkey FOREIGN KEY (forecast_run_id) REFERENCES agri.forecast_run(id);

-- FK CONSTRAINT: forecast_hindcast_run forecast_hindcast_run_release_set_id_fkey
ALTER TABLE ONLY agri.forecast_hindcast_run
    ADD CONSTRAINT forecast_hindcast_run_release_set_id_fkey FOREIGN KEY (release_set_id) REFERENCES agri.release_set(id);

-- FK CONSTRAINT: forecast_hindcast_run forecast_hindcast_run_series_id_fkey
ALTER TABLE ONLY agri.forecast_hindcast_run
    ADD CONSTRAINT forecast_hindcast_run_series_id_fkey FOREIGN KEY (series_id) REFERENCES agri.forecast_series(id);
