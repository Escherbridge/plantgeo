-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_series forecast_series_data_source_id_fkey
ALTER TABLE ONLY agri.forecast_series
    ADD CONSTRAINT forecast_series_data_source_id_fkey FOREIGN KEY (data_source_id) REFERENCES agri.data_source(id);

-- FK CONSTRAINT: forecast_series forecast_series_spatial_cell_id_fkey
ALTER TABLE ONLY agri.forecast_series
    ADD CONSTRAINT forecast_series_spatial_cell_id_fkey FOREIGN KEY (spatial_cell_id) REFERENCES agri.spatial_cell(id);
