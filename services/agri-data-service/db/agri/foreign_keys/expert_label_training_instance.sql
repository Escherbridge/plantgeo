-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: expert_label_training_instance fk_expert_label_training_instance_label
ALTER TABLE ONLY agri.expert_label_training_instance
    ADD CONSTRAINT fk_expert_label_training_instance_label FOREIGN KEY (label_id) REFERENCES agri.expert_label(id);

-- FK CONSTRAINT: expert_label_training_instance fk_expert_label_training_instance_release
ALTER TABLE ONLY agri.expert_label_training_instance
    ADD CONSTRAINT fk_expert_label_training_instance_release FOREIGN KEY (release_id) REFERENCES agri.expert_label_release(id);

-- FK CONSTRAINT: expert_label_training_instance fk_expert_label_training_instance_spatial_cell
ALTER TABLE ONLY agri.expert_label_training_instance
    ADD CONSTRAINT fk_expert_label_training_instance_spatial_cell FOREIGN KEY (spatial_cell_id) REFERENCES agri.spatial_cell(id);
