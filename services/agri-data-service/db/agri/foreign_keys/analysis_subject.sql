-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: analysis_subject fk_analysis_subject_artifact_id_artifact
ALTER TABLE ONLY agri.analysis_subject
    ADD CONSTRAINT fk_analysis_subject_artifact_id_artifact FOREIGN KEY (artifact_id) REFERENCES agri.artifact(id);

-- FK CONSTRAINT: analysis_subject fk_analysis_subject_feature_release_artifact
ALTER TABLE ONLY agri.analysis_subject
    ADD CONSTRAINT fk_analysis_subject_feature_release_artifact FOREIGN KEY (source_feature_id, source_release_id, artifact_id) REFERENCES agri.normalized_source_feature(id, source_release_id, artifact_id);

-- FK CONSTRAINT: analysis_subject fk_analysis_subject_parent_subject_id_analysis_subject
ALTER TABLE ONLY agri.analysis_subject
    ADD CONSTRAINT fk_analysis_subject_parent_subject_id_analysis_subject FOREIGN KEY (parent_subject_id) REFERENCES agri.analysis_subject(id);

-- FK CONSTRAINT: analysis_subject fk_analysis_subject_source_release_id_source_release
ALTER TABLE ONLY agri.analysis_subject
    ADD CONSTRAINT fk_analysis_subject_source_release_id_source_release FOREIGN KEY (source_release_id) REFERENCES agri.source_release(id);

-- FK CONSTRAINT: analysis_subject fk_analysis_subject_supersedes_subject_id_analysis_subject
ALTER TABLE ONLY agri.analysis_subject
    ADD CONSTRAINT fk_analysis_subject_supersedes_subject_id_analysis_subject FOREIGN KEY (supersedes_subject_id) REFERENCES agri.analysis_subject(id);
