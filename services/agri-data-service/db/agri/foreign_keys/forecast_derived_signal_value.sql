-- GENERATED by db/tools/split_schema.py from the Alembic migration head.
-- Canonical declarative source of truth. Do NOT hand-edit to change the
-- live schema: edit here AND add a migration that loads this file
-- (see db/AGENTS.md, 'Forward-load workflow'). Regenerate with
-- db/tools/regenerate.py; the schema-parity test guards drift.

-- FK CONSTRAINT: forecast_derived_signal_value fk_forecast_derived_signal_value_definition
ALTER TABLE ONLY agri.forecast_derived_signal_value
    ADD CONSTRAINT fk_forecast_derived_signal_value_definition FOREIGN KEY (signal_definition_id, max_dependency_depth) REFERENCES agri.forecast_signal_definition(id, max_dependency_depth);

-- FK CONSTRAINT: forecast_derived_signal_value fk_forecast_derived_signal_value_series
ALTER TABLE ONLY agri.forecast_derived_signal_value
    ADD CONSTRAINT fk_forecast_derived_signal_value_series FOREIGN KEY (series_id) REFERENCES agri.forecast_series(id);
