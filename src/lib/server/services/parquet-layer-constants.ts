/** Serving limits owned by the governed Parquet products. */

/** Native soil cells a detail-tier viewport may draw; one extra row detects truncation. */
export const SOIL_FIELD_MAX_CELLS = 4_000;

/** Climate cells a viewport may draw; one extra row detects truncation. */
export const CLIMATE_FIELD_MAX_CELLS = 512;

