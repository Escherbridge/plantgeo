/** Client-safe metadata for first-party, versioned vegetation publications. */
export {
  type NDVIColorStop,
  ENVIRONMENTAL_TILES_CONFIGURED,
  NDVI_COLOR_RAMP,
  NDWI_COLOR_RAMP,
  getCopernicusNDVITileUrl,
  getEnvironmentalTileTemplate,
  getNDVITileUrl,
  getNDWITileUrl,
} from "@/lib/vegetation";
