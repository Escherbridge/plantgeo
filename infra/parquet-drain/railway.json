{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/cron-ingest/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ALWAYS",
    "startCommand": "sh -c 'while true; do agri-service data parquet-drain --layer fire-detections --layer burn-severity --layer signal --layer vegetation --time-budget-seconds 3600 --days-per-lane-turn 50 --progress; sleep 15; done'"
  }
}
