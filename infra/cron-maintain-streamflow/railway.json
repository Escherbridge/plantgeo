{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/cron-ingest/Dockerfile"
  },
  "deploy": {
    "cronSchedule": "47 7 * * *",
    "restartPolicyType": "NEVER",
    "startCommand": "agri-cli jobs-plan-gaps --lane streamflow-archive --apply && agri-cli jobs-reconcile-lane --lane streamflow-archive --apply"
  }
}
