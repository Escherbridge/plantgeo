{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/cron-ingest/Dockerfile"
  },
  "deploy": {
    "cronSchedule": "5,35 * * * *",
    "restartPolicyType": "NEVER",
    "startCommand": "agri-cli jobs-run --lane firms-archive"
  }
}
