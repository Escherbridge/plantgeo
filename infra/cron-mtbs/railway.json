{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/cron-ingest/Dockerfile"
  },
  "deploy": {
    "cronSchedule": "55 7 * * 2",
    "restartPolicyType": "NEVER",
    "startCommand": "agri-service data ingest-mtbs"
  }
}
